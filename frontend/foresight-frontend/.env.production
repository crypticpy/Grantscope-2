# Production environment variables
# Vite automatically loads .env.production during `pnpm build`
# This ensures the Azure backend URL is always baked into production builds.
VITE_API_URL=https://ca-grantscope2-api-prod.agreeablefield-6a1f563b.southcentralus.azurecontainerapps.io

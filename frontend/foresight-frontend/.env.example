# =============================================================================
# Foresight Frontend Environment Configuration
# =============================================================================
# Copy this file to .env.local for local development
# For production (Vercel), configure these in the Vercel Dashboard:
#   Project Settings > Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Your Supabase project URL (found in Project Settings > API)
# Local:      http://localhost:54321 (if using Supabase local dev)
# Production: https://your-project-ref.supabase.co
VITE_SUPABASE_URL=your_supabase_project_url

# Supabase anonymous/public key (safe to expose in frontend)
# Found in Project Settings > API > Project API keys > anon/public
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# The Foresight backend API URL
# Local:      http://localhost:8000
# Production: https://foresight-api.up.railway.app (Railway deployment)
VITE_API_URL=http://localhost:8000

# =============================================================================
# Vercel Deployment Checklist
# =============================================================================
# Before deploying to Vercel, ensure you have configured:
#
# 1. VITE_SUPABASE_URL
#    - Set to your production Supabase project URL
#    - Example: https://abcdefghijkl.supabase.co
#
# 2. VITE_SUPABASE_ANON_KEY
#    - Set to your Supabase anon/public key
#    - This key is safe for frontend use (RLS protects data)
#
# 3. VITE_API_URL
#    - Set to your Railway backend URL once deployed
#    - Example: https://foresight-api.up.railway.app
#
# Environment Scopes:
#    - Production: Used for main branch deployments
#    - Preview: Used for PR preview deployments
#    - Development: Used for local development (optional)
#
# Vercel Dashboard: Project Settings > Environment Variables
# =============================================================================

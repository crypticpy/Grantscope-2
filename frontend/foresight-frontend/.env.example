# =============================================================================
# GrantScope2 Frontend Environment Configuration
# =============================================================================
# Copy this file to .env.local for local development
# For production (Vercel), configure these in the Vercel Dashboard:
#   Project Settings > Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Self-hosted (docker compose):
#   VITE_SUPABASE_URL=http://localhost:3000   (gateway port, see docker-compose.yml)
#   Generate anon key with: python infra/generate_keys.py
#
# Supabase Cloud:
#   VITE_SUPABASE_URL=https://your-project-ref.supabase.co
#   Copy anon key from Project Settings > API > Project API keys
VITE_SUPABASE_URL=http://localhost:3000

# Supabase anonymous/public key (safe to expose in frontend — RLS protects data)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# The GrantScope2 backend API URL
# Local:      http://localhost:8000 (running backend directly or via docker)
# Production: https://grantscope-api.azurecontainerapps.io
VITE_API_URL=http://localhost:8000

# =============================================================================
# Vercel Deployment Checklist
# =============================================================================
# Before deploying, ensure you have configured:
#
# 1. VITE_SUPABASE_URL
#    - Self-hosted: https://grantscope-gateway.azurecontainerapps.io
#    - Supabase Cloud: https://your-project-ref.supabase.co
#
# 2. VITE_SUPABASE_ANON_KEY
#    - Self-hosted: generated by infra/generate_keys.py
#    - Supabase Cloud: from Project Settings > API
#
# 3. VITE_API_URL
#    - Azure: https://grantscope-api.azurecontainerapps.io
#    - Local: http://localhost:8000
# =============================================================================
